apiVersion: apps/v1
kind: Deployment
metadata: 
  name: {{ .Values.web.name | default .Values.web.name }}
  namespace: {{ .Values.namespace | default "default" }}
spec:
  replicas: {{ .Values.web.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.web.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.web.name }}
    spec:
      containers:
      - name: nginx
        image: "{{ .Values.web.image.nginx.repository }}:{{ .Values.web.image.nginx.tag }}"
        ports:
        - containerPort: 80
        resources:
          {{- toYaml .Values.web.resources | nindent 10 }}
      volumes:
      - name: html
        configMap:
          name: {{ .Values.web.name }}-config          